/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ExitWithWarningDirective } from './exit-with-warning.directive';

describe('Directive: ExitWithWarning', () => {
  it('should create an instance', () => {
    const directive = new ExitWithWarningDirective();
    expect(directive).toBeTruthy();
  });
});
